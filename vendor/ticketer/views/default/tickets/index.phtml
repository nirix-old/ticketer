<div class="tickets content">
	<div class="page_actions">
		<?=HTML::link(l('new_ticket'), '/tickets/new')?>
	</div>
	<h2 class="page_title"><?=l('tickets')?></h2>
	<table class="list">
		<thead>
			<tr>
				<th><?=l('id')?></th>
				<th><?=l('summary')?></th>
				<th><?=l('status')?></th>
				<th><?=l('department')?></th>
				<th><?=l('created')?></th>
				<th><?=l('updated')?></th>
			</tr>
		</thead>
		<tbody>
		<?php foreach ($tickets as $ticket): ?>
			<tr>
				<td>#<?=$ticket->id?></td>
				<td><?=HTML::link($ticket->summary, $ticket->href())?></td>
				<td><?=$ticket->status->name?></td>
				<td><?=$ticket->department->name?></td>
				<td><?=timeAgo($ticket->created_at)?></td>
				<td><?=($ticket->updated_at !== null ? timeAgo($ticket->updated_at) : l('never'))?></td>
			</tr>
		<?php endforeach; ?>
		</tbody>
	</table>
</div>
